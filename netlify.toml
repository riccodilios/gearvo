# Netlify build config for Gearvo (Next.js)
# https://docs.netlify.com/configure-builds/file-based-configuration/
# Netlify auto-detects Next.js and uses the OpenNext adapter.

[build]
  command = "npx prisma generate && npx prisma migrate deploy && npm run build"

[build.environment]
  NODE_VERSION = "20"
